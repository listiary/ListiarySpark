Section to Inline List

The `{nlcomment}` or section decorator is removed, a tag is added, and a production arrow.
The coma after the last item of the list we are making is replaced with a semicolon.
Entries are indented one level more.